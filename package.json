{
  "name": "fgl-wiki",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:serve": "vitepress preview docs/.vitepress/dist",
    "deploy": "npm run docs:build && gh-pages -d docs/.vitepress/dist"
  },
  "devDependencies": {
    "vitepress": "^1.0.0",
    "gh-pages": "^5.0.0"
  }
}
