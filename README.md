# FGL-Wiki

已添加 VitePress 静态文档站点脚手架，位置在 `docs/`。

快速命令：

```bash
# 安装依赖
npm install

# 本地开发
npm run docs:dev

# 构建静态站点
npm run docs:build

# 部署到 GitHub Pages（需先配置 repo）
npm run deploy
```

如需我为你配置 GitHub Pages 的 `base` 或 CI/CD（例如 GitHub Actions / Vercel），告诉我你的部署目标。
